# IDINO Career 프로젝트 작업 내역 보고서

**작성일**: 2026-01-24
**작성자**: Claude AI Assistant
**프로젝트**: idino_career (대학생 커리어 개발 플랫폼)

---

## 1. 이전 세션 작업 내역 (요약)

### 1.1 Opportunity Service 수정
- **문제**: `search_path` 미설정으로 테이블 조회 실패
- **해결**: `database.py`에 `search_path` 설정 추가
- **파일**: `services/opportunity-service/app/database.py`

### 1.2 Opportunity Types 스키마 확장
- **문제**: `volunteer`, `certification` 유형 누락
- **해결**: 스키마에 누락된 기회 유형 추가
- **파일**: `services/opportunity-service/app/schemas/opportunity.py`

### 1.3 Student Service GPA 계산 수정
- **문제**: `tb_grade` 테이블에 데이터가 없어 GPA가 0으로 표시
- **해결**: `tb_grade_summary` 테이블을 fallback으로 사용하는 로직 추가
- **결과**: 2023, 2024, 2025년 입학생 모두 GPA 및 학점 정상 표시
- **파일**: `services/student-service/app/services/student_service.py`

---

## 2. 현재 세션 작업 내역

### 2.1 Task 1: Mockup 데이터 존재 여부 확인

#### 확인 결과: 존재함

| 유형 | 위치 | 파일 수 | 내용 |
|------|------|---------|------|
| SQL Seed 파일 | `database/` | 13개 | 학생, 역량, 성적, 코칭, 기회, 배지, 시뮬레이션, 어드바이저 데이터 |
| JSON Mock 파일 | `services/integration-service/app/mock/` | 3개 | worknet, university, hrd 외부 연동 mock |
| 테스트 계정 | `database/scripts/03_seed_test_data.sql` | - | admin/admin123, student_hong/student123 등 |

#### Mock 데이터 상세 목록
```
database/03_seed_data.sql          - 기본 시드 데이터
database/04_seed_students.sql      - 학생 데이터
database/05_seed_enrollments.sql   - 수강 데이터
database/06_seed_p1_skills.sql     - 스킬 데이터
database/07_seed_p1_coaching.sql   - 코칭 데이터
database/08_seed_p1_opportunities.sql - 기회 데이터
database/09_seed_p1_risk.sql       - 리스크 데이터
database/10_seed_p2_badges.sql     - 배지 데이터
database/11_seed_p2_simulation.sql - 시뮬레이션 데이터 (기존)
database/12_seed_p2_advisor.sql    - 어드바이저 데이터
```

#### Mock 모드 제어
- 환경변수: `USE_MOCK_DATA=true/false`
- 위치: `services/integration-service/app/config.py`

---

### 2.2 Task 2: What-if 시뮬레이션 실제 데이터 생성

#### 웹 검색 수행
- 2024-2025 한국 대학생 인기 인턴십 프로그램 (삼성, SK, LG, 현대)
- IT 개발자 자격증 현황 (정보처리기사, AWS 클라우드)
- 한국 대학생 진로/취업 경로 (데이터사이언스, AI 엔지니어)

#### 생성 파일
- **파일명**: `database/13_seed_realistic_simulation.sql`
- **데이터 수**: 49개 시뮬레이션 시나리오

#### 시나리오 유형별 분포

| 시나리오 유형 | 개수 | 예시 |
|--------------|------|------|
| career_path | 39개 | 삼성 DS/DX 인턴, 데이터사이언티스트, AI엔지니어, 백엔드/프론트엔드 개발자 |
| opportunity | 4개 | 삼성전자 인턴십, SK하이닉스 인턴십, AI 스타트업, 오픈소스 프로젝트 |
| skill_development | 3개 | AWS 자격증, 정보처리기사, Python 데이터분석 |
| course_selection | 2개 | AI/ML 과목 집중, 웹개발 풀스택 |
| timeline | 1개 | 2027년 2월 취업 준비 완료 |

#### 시나리오 데이터 특징
- **실제 채용 정보 반영**: 삼성 서류접수 일정(8/27~9/3), 합격률, 연봉 데이터
- **실제 자격증 정보**: 정보처리기사 시험일정, 합격률(필기 35%, 실기 45%)
- **AI 분석 결과 포함**: 강점, 리스크, 추천사항, 다음 단계 상세 포함
- **신뢰도 점수**: 각 시나리오별 confidence_level 0.65~0.92

#### FK 오류 수정
- **문제**: `2023001001` 등 가상 학생 ID 사용으로 FK 위반
- **해결**: 실제 DB의 학생 ID(`20002268`, `20002269`, `20002270`, `20002271`)로 변경
- **추가 수정**: department_cd 조건 제거 (실제 DB는 숫자 코드 사용)

---

### 2.3 Task 3: AI 관련 기능 검증

#### AI Service (Port 8006) 코드 구조 확인

| 파일 | 역할 |
|------|------|
| `app/main.py` | FastAPI 앱 진입점, CORS, 라이프사이클 |
| `app/config.py` | 환경설정 (OpenAI, 서비스 URL, DB) |
| `app/routers/ai_router.py` | AI 엔드포인트 정의 |
| `app/services/recommendation_service.py` | 추천 로직, 외부 서비스 연동 |
| `app/services/llm_service.py` | LLM 호출, Tool Calling, RAG |
| `app/tools/tool_definitions.py` | OpenAI Tool 정의, JSON Schema |
| `app/tools/tool_executor.py` | Tool 실행기 |

#### AI Service 엔드포인트

| 엔드포인트 | 메서드 | 기능 |
|-----------|--------|------|
| `/ai/actions/{student_id}` | GET | 4개 맞춤형 액션 추천 |
| `/ai/analyze` | POST | 역량 분석 |
| `/ai/chat` | POST | AI 챗봇 상담 |
| `/ai/heatstrip/{student_id}` | GET | 12개월 역량 변화 히트맵 |
| `/ai/sprint/{student_id}` | GET | 학기 스프린트 목표 |
| `/ai/recommendations/tools` | POST | Tool Calling 기반 추천 |
| `/ai/recommendations/rag` | POST | RAG + Tool Calling 통합 추천 |
| `/eval/feedback` | POST | 피드백 저장 |
| `/eval/metrics` | GET | 평가 메트릭 |
| `/eval/cases` | GET | 평가 케이스 목록 |

#### Simulation Service (Port 8013) 코드 구조 확인

| 파일 | 역할 |
|------|------|
| `app/main.py` | FastAPI 앱 진입점 |
| `app/config.py` | 환경설정 |
| `app/routers/simulation_router.py` | 시뮬레이션 엔드포인트 |
| `app/services/simulation_service.py` | 시뮬레이션 로직, AI 분석 |
| `app/schemas/simulation.py` | Pydantic 스키마 |

#### Simulation Service 엔드포인트

| 엔드포인트 | 메서드 | 기능 |
|-----------|--------|------|
| `/simulation/types` | GET | 시나리오 유형 목록 |
| `/simulation/guide/{type}` | GET | 유형별 상세 가이드 |
| `/simulation/suggestions/{student_id}` | GET | 맞춤 추천 시나리오 |
| `/simulation/scenarios` | GET/POST | 시나리오 CRUD |
| `/simulation/scenarios/{id}` | GET/DELETE | 시나리오 상세/삭제 |
| `/simulation/compare` | POST | 시나리오 비교 |
| `/simulation/career-path` | POST | 진로 경로 시뮬레이션 |
| `/simulation/skill-development` | POST | 스킬 개발 시뮬레이션 |
| `/simulation/course-selection` | POST | 과목 선택 시뮬레이션 |
| `/simulation/timeline` | POST | 타임라인 시뮬레이션 |

#### 환경 설정 확인

**AI Service (.env)**
```env
OPENAI_API_KEY=sk-proj-***  (설정됨)
OPENAI_MODEL=gpt-4o-mini
STUDENT_SERVICE_URL=http://localhost:8002
COMPETENCY_SERVICE_URL=http://localhost:8003
ALUMNI_SERVICE_URL=http://localhost:8005
```

**Simulation Service (.env)**
```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=2012
DB_SCHEMA=idino_career
OPENAI_API_KEY=sk-proj-***  (설정됨)
OPENAI_MODEL=gpt-4o-mini
```

---

### 2.4 서비스 실행 및 테스트

#### 시드 데이터 적용
```bash
psql -h localhost -U postgres -d postgres -f database/13_seed_realistic_simulation.sql
```
**결과**: 49개 시나리오 INSERT 성공

#### 서비스 실행
```bash
# AI Service
cd services/ai-service
python -m uvicorn app.main:app --host 0.0.0.0 --port 8006 --reload

# Simulation Service
cd services/simulation-service
python -m uvicorn app.main:app --host 0.0.0.0 --port 8013 --reload
```

#### API 테스트 결과

| 서비스 | 엔드포인트 | 상태 | 응답 |
|--------|-----------|------|------|
| AI | `/health` | ✅ 성공 | `{"status":"healthy","service":"ai-service"}` |
| AI | `/ai/actions/20002268` | ✅ 성공 | 4개 맞춤형 액션 추천 (창의력, 소통, 글로벌역량, 도전) |
| AI | `/ai/heatstrip/20002268` | ✅ 성공 | 12개월 역량 변화 데이터 (창의, 융복합, 소통, 협력) |
| AI | `/ai/sprint/20002268` | ✅ 성공 | 5개 목표 (60% 완료율), AI 조언 2개 |
| Simulation | `/health` | ✅ 성공 | `{"status":"healthy","service":"simulation-service"}` |
| Simulation | `/simulation/types` | ✅ 성공 | 5개 시나리오 유형 |
| Simulation | `/simulation/scenarios?student_id=20002268` | ✅ 성공 | 학생별 시나리오 목록 |
| Simulation | `/simulation/guide/career_path` | ✅ 성공 | 진로 경로 시뮬레이션 가이드 |

---

## 3. 현재 시스템 상태

### 3.1 실행 중인 서비스

| 서비스 | 포트 | PID | 상태 |
|--------|------|-----|------|
| AI Service | 8006 | 18496 | ✅ Running |
| Simulation Service | 8013 | 26680 | ✅ Running |

### 3.2 데이터베이스 상태

| 테이블 | 레코드 수 | 비고 |
|--------|----------|------|
| tb_simulation_scenario (REALISTIC_SEED) | 49개 | 실제 한국 취업시장 데이터 기반 |
| tb_student | 다수 | 실제 학생 데이터 존재 |
| tb_competency_score | 다수 | 역량 점수 데이터 존재 |

### 3.3 API 문서 접근 경로

| 서비스 | Swagger UI | ReDoc |
|--------|-----------|-------|
| AI Service | http://localhost:8006/docs | http://localhost:8006/redoc |
| Simulation Service | http://localhost:8013/docs | http://localhost:8013/redoc |

---

## 4. 생성/수정된 파일 목록

### 4.1 새로 생성된 파일

| 파일 경로 | 용도 |
|----------|------|
| `database/13_seed_realistic_simulation.sql` | 실제 한국 취업시장 기반 시뮬레이션 시드 데이터 |
| `services/integration-service/app/database.py` | Integration Service DB 연결 모듈 |
| `claudedocs/20260124_작업내역_보고서.md` | 본 보고서 |

### 4.2 수정된 파일 (현재 세션)

| 파일 경로 | 수정 내용 |
|----------|----------|
| `services/integration-service/app/config.py` | DB 연결 설정 추가 |
| `services/integration-service/app/connectors/university_connector.py` | DB 쿼리 기반 mock 데이터 조회 |
| `services/integration-service/app/connectors/hrd_connector.py` | DB 쿼리 기반 mock 데이터 조회 |
| `services/integration-service/requirements.txt` | sqlalchemy, asyncpg 의존성 추가 |

### 4.3 수정된 파일 (이전 세션)

| 파일 경로 | 수정 내용 |
|----------|----------|
| `services/opportunity-service/app/database.py` | search_path 설정 추가 |
| `services/opportunity-service/app/schemas/opportunity.py` | volunteer, certification 타입 추가 |
| `services/student-service/app/services/student_service.py` | GPA 계산 fallback 로직 추가 |

---

## 5. 알려진 이슈 및 권장사항

### 5.1 해결된 이슈
- ✅ Opportunity Service search_path 미설정
- ✅ 기회 유형 스키마 누락
- ✅ Student Service GPA 계산 오류
- ✅ 시뮬레이션 시드 데이터 FK 위반
- ✅ Integration Service Mock 데이터 DB 연동 (University, HRD Connector)

### 5.2 잠재적 이슈
- Tool Calling 엔드포인트 (`/ai/recommendations/tools`) JSON 파싱 오류 발생 가능
  - 원인: Windows 환경에서 curl의 JSON 인코딩 문제 추정
  - 권장: Postman 또는 프론트엔드에서 테스트

### 5.3 권장사항
1. **서비스 의존성**: AI 서비스는 Student(8002), Competency(8003), Alumni(8005) 서비스에 의존
2. **Mock 데이터 활용**: 의존 서비스 미실행 시 자동으로 Mock 데이터 반환
3. **RAG 기능**: DB 연결 시 RAG 기반 Evidence 검색 활성화됨
4. **정기 백업**: 시뮬레이션 시나리오 데이터 정기 백업 권장

---

### 2.5 Task 4: Integration Service - DB 기반 Mock 데이터 전환

#### 작업 목표
- Worknet 데이터를 제외한 모든 Mock 데이터를 JSON 파일 대신 DB에서 student_id로 조회
- University Connector: `tb_student`, `tb_grade_summary`, `tb_grade`, `tb_course` 테이블 사용
- HRD Connector: `tb_student`, `tb_grade_summary`, `tb_alumni_cohort` 테이블 사용

#### 생성/수정된 파일

| 파일 | 변경 내용 |
|------|----------|
| `services/integration-service/app/config.py` | DB 연결 설정 추가 (DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD, DB_SCHEMA) |
| `services/integration-service/app/database.py` | **신규** - SQLAlchemy async 데이터베이스 연결 모듈 |
| `services/integration-service/app/connectors/university_connector.py` | JSON mock → DB 쿼리로 변경 (`_get_student_from_db` 메서드 추가) |
| `services/integration-service/app/connectors/hrd_connector.py` | JSON mock → DB 쿼리로 변경 (`_get_alumni_from_db` 메서드 추가) |
| `services/integration-service/requirements.txt` | `sqlalchemy[asyncio]`, `asyncpg` 의존성 추가 |

#### University Connector 데이터 소스

```sql
-- 학생 기본 정보
tb_student: student_id, student_nm, department_cd, admission_year, current_grade, status

-- GPA/학점 계산
tb_grade_summary: student_id, term_cd, gpa, earned_credits

-- 수강 과목 및 성적
tb_grade: student_id, course_cd, term_cd, grade_letter, grade_point, credits_earned
tb_course: course_cd, course_nm, department_cd, credits, course_type
```

#### HRD Connector 데이터 소스

```sql
-- 졸업생/예정자 정보
tb_student: student_id, department_cd, admission_year, status (32/41/42/51: 졸업 예정/졸업)

-- 졸업생 GPA 계산
tb_grade_summary: student_id, gpa, earned_credits

-- 취업 통계 정보
tb_alumni_cohort: department_cd, graduation_year, employment_rate, avg_salary, top_employers, top_roles
```

#### 테스트 결과

| Connector | 메서드 | 결과 | 비고 |
|-----------|--------|------|------|
| University | `_get_student_from_db('20002268')` | ✅ 성공 | GPA=0.0 (grade_summary 데이터 없음), Courses=8 |
| University | `_get_student_from_db('20011393')` | ✅ 성공 | GPA=3.25, Credits=8 |
| HRD | `_get_alumni_from_db(limit=3)` | ✅ 성공 | 3명 졸업예정자 조회 |

#### 참고사항
- `USE_MOCK_DATA=true` 설정 시 JSON 파일 대신 DB 쿼리 사용
- Worknet Connector는 기존 JSON mock 유지 (외부 API 연동용)
- asyncpg의 NULL 파라미터 처리를 위해 동적 SQL 생성 패턴 적용

---

## 6. 다음 단계 제안

1. **프론트엔드 통합 테스트**: AI/Simulation API와 프론트엔드 연동 확인
2. **E2E 테스트**: 전체 시나리오 플로우 검증
3. **성능 테스트**: 다수 사용자 동시 접속 시 응답 시간 측정
4. **모니터링 설정**: 서비스 상태 모니터링 및 알림 설정
5. **Integration Service 실행 테스트**: Port 8019에서 서비스 실행 및 API 검증

---

## 7. 세션 3 작업 내역 (2026-01-25)

### 7.1 문제 상황 분석

#### 발견된 문제
1. **AI 액션보드 "학기 스프린트"**: 하드코딩된 목업 데이터 사용
2. **스프린트 메뉴 페이지**: localStorage만 사용, 백엔드 API 미연동
3. **데이터 불일치**: 두 화면에서 동일한 데이터가 표시되지 않음

#### 근본 원인
- `services/ai-service/.env` 파일에 데이터베이스 연결 설정 누락
- `recommendation_service.py`의 `_fetch_student_goals()` 메서드가 DB 연결 실패 시 빈 배열 반환
- `SprintBoardSection.tsx`가 localStorage만 사용하고 API 호출 없음

---

### 7.2 학기 스프린트 데이터 생성 (SQL)

**파일**: `scripts/generate_sprint_goals.sql`

#### 학과별 목표 분류
| 계열 | 포함 학과 |
|------|----------|
| IT/공학 | 컴퓨터공학과, AI소프트웨어학과, 정보통신공학과, 전자공학과, 기계공학과, 산업공학과 |
| 의료/보건 | 간호학과, 물리치료학과, 보건행정학과, 치위생학과 |
| 경영/사회 | 경영학과, 경제학과, 국제통상학과, 행정학과, 사회복지학과 |
| 교육 | 유아교육학과, 특수교육학과, 교육학과 |
| 예술/디자인 | 시각디자인학과, 미술학과, 음악학과, 공연예술학과 |
| 기타 | 그 외 학과 |

#### 학년별 목표 특성
| 학년 | 입학년도 | 특성 | 예시 목표 |
|------|---------|------|----------|
| 1학년 | 2025 | 기초 역량 확립 | 전공 기초 과목 이수, 영어 기초 실력 향상, 진로 탐색 멘토링 참여 |
| 2학년 | 2024 | 전문성 심화 | 전공 심화 프로젝트, 자격증 필기 준비, 교내 공모전 참가 |
| 3학년 | 2023 | 실무 경험 축적 | 자격증 실기 합격, 인턴십 지원, 캡스톤 프로젝트 |

#### 생성 결과
```
Total goals generated: 22,515건
├── 2025학번 (1학년): 7,869건
├── 2024학번 (2학년): 7,368건
└── 2023학번 (3학년): 7,278건

평균 학생당 목표: 3.0~3.1건
```

---

### 7.3 AI Service 데이터베이스 연결 설정

**파일**: `services/ai-service/.env`

#### 추가된 환경 변수
```env
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=2012
DB_SCHEMA=idino_career
```

#### 문제점과 해결
- **문제**: 기존 `.env` 파일에 데이터베이스 설정이 누락
- **결과**: `_fetch_student_goals()` 메서드 연결 실패 → 빈 배열 반환 → 목업 데이터 표시
- **해결**: 데이터베이스 연결 설정 추가 후 서비스 재시작

---

### 7.4 RecommendationService 수정

**파일**: `services/ai-service/app/services/recommendation_service.py`

#### 추가된 메서드: `_fetch_student_goals()`
```python
async def _fetch_student_goals(self, student_id: str) -> List[Dict[str, Any]]:
    """Fetch student goals from tb_coaching_goal table."""
    try:
        import asyncpg
        conn = await asyncpg.connect(
            host=self.settings.DB_HOST,
            port=self.settings.DB_PORT,
            database=self.settings.DB_NAME,
            user=self.settings.DB_USER,
            password=self.settings.DB_PASSWORD,
        )
        try:
            rows = await conn.fetch(
                """
                SELECT title, description, priority, status, progress_percentage
                FROM idino_career.tb_coaching_goal
                WHERE std_id = $1
                ORDER BY
                    CASE priority
                        WHEN 'high' THEN 1
                        WHEN 'medium' THEN 2
                        ELSE 3
                    END,
                    created_at DESC
                LIMIT 5
                """,
                student_id
            )
            goals = []
            for row in rows:
                goals.append({
                    "label": row["title"],
                    "description": row["description"] or "",
                    "completed": row["status"] == "completed" or row["progress_percentage"] == 100,
                    "priority": row["priority"] or "medium",
                    "progress": row["progress_percentage"] or 0,
                })
            return goals
        finally:
            await conn.close()
    except Exception as e:
        print(f"Failed to fetch goals from database: {e}")
        return []
```

#### 수정된 메서드: `get_semester_sprint()`
- 데이터베이스에서 목표를 먼저 조회
- 조회 실패 시에만 목업 데이터로 폴백

---

### 7.5 SprintBoardSection 수정

**파일**: `frontend/components/sections/SprintBoardSection.tsx`

#### 주요 변경 사항

1. **API 연동 추가**
   - `aiService.getSemesterSprint()` 호출하여 데이터베이스에서 목표 가져오기
   - localStorage가 비어있을 때만 API 호출

2. **데이터 변환 함수 추가**
   ```typescript
   const convertGoalToSprintItem = (goal: SemesterGoal, index: number): SprintItem => ({
     id: generateId(),
     title: goal.label,
     description: (goal as { description?: string }).description || undefined,
     item_type: 'goal' as SprintItemType,
     status: goal.completed ? 'done' : 'todo',
     priority: goal.priority,
     order_index: index,
     created_at: new Date().toISOString(),
   });
   ```

3. **새로고침 버튼 추가**
   - API에서 최신 데이터를 다시 가져오는 기능
   - `RefreshCw` 아이콘 사용
   - 로딩 중 회전 애니메이션

4. **에러 처리**
   - API 호출 실패 시 에러 메시지 표시
   - 닫기 버튼으로 에러 메시지 dismiss 가능

5. **초기화 상태 추적**
   - `sprint_initialized_${studentId}_${semester}` 키로 초기화 여부 추적
   - 사용자가 의도적으로 모든 항목을 삭제한 경우 재호출 방지

---

### 7.6 테스트 결과

#### 7.6.1 AI 액션보드 - 학기 스프린트

**테스트 학생**: 20254364 (경영학과, 1학년)

**API 응답** (`GET /ai/sprint/20254364`):
```json
{
  "student_id": "20254364",
  "semester": "2026-1학기",
  "goals": [
    {"label": "전공 기초 과목 이수", "completed": false, "priority": "high"},
    {"label": "진로 탐색 멘토링 참여", "completed": true, "priority": "medium"},
    {"label": "영어 기초 실력 향상", "completed": false, "priority": "medium"}
  ],
  "completion_rate": 0.333,
  "ai_suggestions": ["매주 최소 2시간씩 전공 과목 복습...", "문제해결 능력을 향상시키기 위해..."]
}
```

**화면 표시**: 1/3 달성
- ⚡ 전공 기초 과목 이수 (높음, 미완료)
- ✓ 진로 탐색 멘토링 참여 (완료)
- 영어 기초 실력 향상 (미완료)

#### 7.6.2 스프린트 메뉴 페이지

**칸반 보드 표시**:

| 할 일 (2) | 진행 중 (0) | 완료 (1) |
|-----------|-------------|----------|
| 전공 기초 과목 이수 (높음) | - | 진로 탐색 멘토링 참여 (보통) |
| 영어 기초 실력 향상 (보통) | | |

**통계**:
- 전체 항목: 3
- 진행 중: 0
- 완료: 1
- 완료율: 33%

---

### 7.7 파일 변경 목록

| 파일 경로 | 변경 유형 | 설명 |
|-----------|----------|------|
| `scripts/generate_sprint_goals.sql` | 신규 | 학과별/학년별 목표 데이터 생성 SQL (22,515건) |
| `services/ai-service/.env` | 수정 | 데이터베이스 연결 설정 추가 |
| `services/ai-service/app/services/recommendation_service.py` | 수정 | `_fetch_student_goals()` 메서드 추가, `get_semester_sprint()` 수정 |
| `frontend/components/sections/SprintBoardSection.tsx` | 수정 | API 연동, 새로고침 버튼, 에러 처리 추가 |

---

### 7.8 작동 흐름도

```
┌─────────────────────────────────────────────────────────────────┐
│                    대시보드 - AI 액션보드                          │
├─────────────────────────────────────────────────────────────────┤
│  ActionBoardSection.tsx                                         │
│       │                                                         │
│       ▼                                                         │
│  aiService.getSemesterSprint(studentId)                         │
│       │                                                         │
│       ▼                                                         │
│  AI Service API: /ai/sprint/{studentId}                         │
│       │                                                         │
│       ▼                                                         │
│  recommendation_service.get_semester_sprint()                   │
│       │                                                         │
│       ▼                                                         │
│  _fetch_student_goals() ──► PostgreSQL: tb_coaching_goal        │
│       │                                                         │
│       ▼                                                         │
│  학기 스프린트 섹션에 목표 표시                                    │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    스프린트 메뉴 페이지                            │
├─────────────────────────────────────────────────────────────────┤
│  SprintBoardSection.tsx                                         │
│       │                                                         │
│       ▼                                                         │
│  localStorage 확인                                               │
│       │                                                         │
│       ├── 데이터 있음 ──► localStorage에서 로드                   │
│       │                                                         │
│       └── 데이터 없음 ──► aiService.getSemesterSprint()          │
│                │                                                │
│                ▼                                                │
│           API 응답을 SprintItem 형식으로 변환                     │
│                │                                                │
│                ▼                                                │
│           localStorage에 저장 + 칸반 보드 표시                    │
│                                                                 │
│  [새로고침 버튼] ──► API 재호출 ──► 최신 데이터로 갱신             │
└─────────────────────────────────────────────────────────────────┘
```

---

### 7.9 향후 고려사항

1. **실시간 동기화**: 현재는 스프린트 페이지에서 변경한 내용이 데이터베이스에 저장되지 않음 (localStorage만 사용)
2. **양방향 동기화**: 칸반 보드의 상태 변경 시 `tb_coaching_goal` 테이블 업데이트 API 구현 필요
3. **주차별 목표**: 현재는 학기 전체 목표만 표시. 주차별 세분화된 목표 기능 추가 고려
4. **알림 기능**: 마감일 임박 목표에 대한 알림 기능 추가 고려

---

**문서 끝**

**최종 수정**: 2026-01-25 00:50 KST
